Tool to track time while working.

Reads status of the current timestamp from `status.txt`.
(Status can be either open or closed).

Keeps track of historic timestamps in `timestamps.dat`

# TODOs
Check TODOS.md
